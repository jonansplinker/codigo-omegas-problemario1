#include <stdio.h>

int main(int argc, char *argv[]) {
	int m,l,p,z,mm,k,t,cofre;
	scanf("%d" "%d" "%d" "%d" "%d" "%d",&m,&l,&p,&z,&mm,&k);
	t=m*l*p;
	cofre=z*mm*k;
	if(cofre=t || cofre>t){
		printf("1");
	}
	else{
		printf("0");
	}
	return 0;
}